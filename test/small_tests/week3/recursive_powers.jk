/*
The function powerN given a base and a n, will recursively find the nth power
of the base
Value: 16
*/
powerN2(currentval, base, n) {
  if (*n >= 2) {
    currentval = *currentval * *base;
    printInt(*currentval);
    powerN2(*currentval, *base, *n - 1);
  } else {
    *currentval;
  }
}

powerN(base, n) {
  powerN2(*base, *base, *n);
}

main() {
  powerN(2, 4);
}
