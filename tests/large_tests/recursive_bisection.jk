bisect (f; a; b; tol) {
  int c = 0;
  int root = 0;
  if (*b - *a >= *tol) {
    c = *a + *b;
    c = *c / 2;
    if (f(c) == 0) {
      root = *c
    } else {
      if (f(*c) <= -1 & f(*a) <= -1 | f(*c) >= 1 & f(*a) >= 1) {
        a = *c
      } else {
        b = *c
      };
      root = bisect(*f;*a;*b;*tol)
    }
  } else {
    root = *c
  }
}
